<%= simple_form_for [@chatroom, @message], remote: true do |f| %>
  <%= f.input :content, label: false, placeholder: "Type your message here...", autofocus: true %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token -%>
<% end %>
